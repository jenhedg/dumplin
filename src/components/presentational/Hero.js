import React from 'react';

// / TODO: Add Styles

const Hero = ({ title }) => {
  return (
    <div className="hero">
      <div className="hero__title">{title}</div>
    </div>
  );
};

export default Hero;
