import styled from 'styled-components';

export default FilterBox => styled(FilterBox)`
  font-size: 1.4em;

  input {
    padding: 1em 0.5em;
    font-size: 0.8em;
    margin-left: 0.5em;
    width: 300px;
  }
`;
